# Secrets for the agent-doc agent only (gitignored).
# Copy to .env (next to this file) and fill in — referenced from agent.ts
# mcpServers headers as ${VAR}. Shell env wins, then this file, then the
# project/global .env.
DOC_MCP_TOKEN=
