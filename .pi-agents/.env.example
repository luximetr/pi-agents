# Per-project secrets for this project's MCP servers.
# Copy to .env (gitignored) and fill in — referenced from config.json as ${VAR}.
# The shell environment takes precedence over this file.
DOC_MCP_TOKEN=
