{
	"defaultAgent": null,
	"keybindings": {
		"select": "ctrl+shift+a",
		"rotate": "ctrl+shift+q"
	},
	"mcpServers": {
		"filesystem": {
			"command": "npx",
			"args": ["-y", "@modelcontextprotocol/server-filesystem@latest", "."]
		}
	}
}
